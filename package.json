{
  "name": "kis",
  "version": "1.0.0",
  "description": "kis for recycled plastics industry",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "electron .",
    "packager": "electron-packager . kis --out=./build --ignore=\".idea|.vscode\" --platform=win32 --arch=ia32 --icon=./images/icon_64px.ico --app-version=1.0.0 --electron-version=4.0.1 --overwrite && node build/build.js"
  },
  "author": "Shenjin",
  "license": "ISC",
  "devDependencies": {
    "electron": "^2.0.16",
    "mongodb": "^3.1.10",
    "mongoose": "^5.4.1",
    "electron-packager": "^13.0.1",
    "electron-winstaller": "^2.7.0"
  },
  "dependencies": {
    "electron-squirrel-startup": "^1.0.0",
    "electron-timber": "^0.5.1",
    "mongodb": "^3.1.10",
    "mongoose": "^5.4.1"
  }
}
