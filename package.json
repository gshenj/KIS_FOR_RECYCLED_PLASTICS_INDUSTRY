{
  "name": "kis",
  "version": "1.0.0",
  "description": "kis for recycled plastics industry",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "electron .",
    "packager": "electron-packager . kis --overwrite --win=x32 --out  ./build/out --arch=x64 --app-version=1.0.0 --electron-version=2.0.16"
  },
  "author": "Shenjin",
  "license": "ISC",
  "devDependencies": {
    "electron": "^2.0.16",
    "mongodb": "^3.1.10",
    "mongoose": "^5.4.1",
    "electron-packager": "^13.0.1",
    "electron-winstaller": "^2.7.0"
  },
  "dependencies": {
    "electron-squirrel-startup": "^1.0.0",
    "mongodb": "^3.1.10",
    "mongoose": "^5.4.1"
  }
}
